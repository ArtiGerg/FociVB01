package FociVB;

/*public class MatchesFactory {

    public static Matches createMatches (String line) {
        String[] parts = line.split(";");
        if (parts.length>5) {
            return new MatchesKiesesesSzakasz(line);
        }else {
            return new Matches(line);
        }
    }

 */

